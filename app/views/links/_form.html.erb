<%= form_with(model: link, local: true) do |form| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
        <% link.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form">
    <div class="form-group">
      <%= form.label :url %>
      <%= form.text_field :url, class: "form-control mt-2 mb-2", id: 'url' %>
    </div>
    <div class="form-group">
      <%= form.label :slug %>
      <%= form.text_field :slug, class: "form-control mt-2 mb-2", id: 'slug' %>
    </div>
    <div class="form-group">
      <div class="btn btn-primary mt-2 mb-2">
        <%= form.submit "Create Teeny Link" %>
      </div>
    </div>
  </div>
<% end %>


